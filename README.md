# Telegram Remote Shell Bot

Інтерактивний Telegram-бот для керування консольними програмами через чат.

## Можливості

- 🚀 Запуск будь-яких консольних програм
- 📤 Передача виводу програми в реальному часі
- ✍️ Інтерактивний ввід через Telegram
- 🛑 Зупинка запущених процесів
- 📊 Перевірка статусу

## Встановлення

1. Встановіть залежності:
```bash
pip3 install -r requirements.txt
```

2. Створіть бота через [@BotFather](https://t.me/BotFather):
   - Надішліть `/newbot`
   - Вкажіть ім'я та username
   - Скопіюйте токен

3. Дізнайтеся свій Chat ID через [@userinfobot](https://t.me/userinfobot)

4. Створіть `.env` файл:
```bash
cp .env.example .env
nano .env
```

Вставте свої дані:
```
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
ADMIN_CHAT_ID=123456789
```

## Запуск

```bash
python3 bot.py
```

Або у фоні:
```bash
nohup python3 bot.py > bot.log 2>&1 &
```

## Використання

1. Відкрийте бота в Telegram
2. Натисніть `/start`
3. Натисніть кнопку **🚀 Start Program**
4. Введіть команду (наприклад: `airodump-ng wlan0mon`)
5. Вивід програми буде надходити в чат
6. Надсилайте текст для вводу в програму
7. Натисніть **🛑 Stop Program** для зупинки

## Приклади команд

- `airodump-ng wlan0mon` - моніторинг Wi-Fi
- `nmap -sV 192.168.1.0/24` - сканування мережі
- `python3 script.py` - запуск Python-скрипта
- `bash` - інтерактивний shell

## Безпека

⚠️ **ВАЖЛИВО:**
- Тримайте `.env` файл у секреті
- Не додавайте його в git (вже в .gitignore)
- Тільки ваш Chat ID має доступ до бота
- Не запускайте недовірені команди

## Системний сервіс (опціонально)

Створіть `/etc/systemd/system/tg-remote-shell.service`:
```ini
[Unit]
Description=Telegram Remote Shell Bot
After=network.target

[Service]
Type=simple
User=kali
WorkingDirectory=/home/kali/tg-remote-shell
ExecStart=/usr/bin/python3 /home/kali/tg-remote-shell/bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Увімкніть:
```bash
sudo systemctl daemon-reload
sudo systemctl enable tg-remote-shell.service
sudo systemctl start tg-remote-shell.service
```
